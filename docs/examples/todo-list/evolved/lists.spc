# Lists

## Default Lists
@label(default-lists)

Every new user starts with a single list called "Inbox."
The Inbox list cannot be deleted or renamed.

## Custom Lists

Users can create custom lists with a name and an optional color (chosen from a preset palette).
List names must be unique per user (case-insensitive, whitespace-trimmed) and are limited to 50 characters.
Examples of common custom lists: "Work," "Personal," "Shopping," "Errands."

## List Management

Users can rename, reorder, and delete custom lists (the Inbox list cannot be modified).
**Deleting a custom list requires confirmation and moves all its tasks to Inbox**
rather than deleting them.

## List Switching
@label(list-switching)

The list selector is accessible from the task list view.
It shows all lists with an unfinished-task count badge next to each name.
Switching lists replaces the task list content with a 200ms crossfade transition.
@see(tasks@task-list)
